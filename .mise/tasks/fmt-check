#!/usr/bin/env bash
#MISE description="Check the Rust formatting"
set -euo pipefail

root=$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)
cargo="${CARGO:-cargo}"

"$cargo" fmt --all --manifest-path "$root/rust/Cargo.toml" --check
