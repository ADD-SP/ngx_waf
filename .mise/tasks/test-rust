#!/usr/bin/env bash
#MISE description="Run the Rust unit tests"
set -euo pipefail

root=$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)
cargo="${CARGO:-cargo}"

"$cargo" test --workspace --manifest-path "$root/rust/Cargo.toml"
